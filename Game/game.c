//
// Created by jurci on 9. 1. 2019.
//

#include "game.h"


//void *initGame(void *arg) {
//
//    GameServers game = *((GameServers *) arg);
//    pthread_t acceptSocketThread;
//    _Bool end = false;
////    while () {
////
////    }
////    if (socketReady()) {
////
////    }
////    while (!end) {
////
////        if (socketReady()) {
////
////        }
//
////}
//
////    pthread_mutex_init(&gameServers[gameIndex].gamelock, NULL);
//
////    pthread_create(&acceptSocketThread, NULL, &acceptGameSocketPLayer, &game.playerCount);
//
//
////    pthread_join(acceptSocketThread, NULL);
//
//
////log_info("Exit Game ");
////pthread_exit(NULL);
//}





